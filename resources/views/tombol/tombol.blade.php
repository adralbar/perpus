<a class="btnDetail p-2 btn btn-success text-white rounded text-sm font-semibold mx-1" href="#"
    data-npk="{{ $data->npk }}" data-nama="{{ $data->nama }}" data-total="{{ $data->total_keterlambatan }}"
    data-tanggal="{{ $data->tanggal_keterlambatan }}" data-waktu="{{ $data->waktu_keterlambatan }}">
    Detail
</a>
